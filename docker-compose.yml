version: '3.8'

services:
  api:
    build: .
    container_name: unhan_data_pipeline_api
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      # Mount folder lokal agar bisa live-reload tanpa rebuild container (opsional saat dev)
      - .:/app
